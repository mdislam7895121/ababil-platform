YOU ARE THE IMPLEMENTATION AGENT.
TASK: STEP 30 — UNBLOCK REAL CI PROOF (NO SIMULATION)

CONTEXT
- STEP 30 code is implemented but NOT complete because real CI proof is blocked.
- Missing CI configuration and secrets:
  - API side needs: GITHUB_TOKEN, GITHUB_REPO, CI_CALLBACK_TOKEN
  - GitHub Actions secrets need: EXPO_TOKEN, API_BASE_URL, CI_CALLBACK_TOKEN
- Goal now: make CI runnable and collect real proof outputs.

NON-NEGOTIABLE RULES
1) Do not claim “complete” until CI runs produce real build proof.
2) No simulation allowed for proof.
3) Provide raw outputs only: curl, GitHub run logs excerpt, and artifacts evidence.
4) Never print secrets. Show only secret names and where they are set.

DELIVERABLE 1 — EXACT SETUP INSTRUCTIONS (OWNER ACTIONS)
Write a single checklist with exact steps for GitHub:
A) Repo Variables (Settings → Secrets and variables → Actions → Variables)
  - GITHUB_REPO = owner/repo   (if used as variable; otherwise store as server env)
B) Repo Secrets (Settings → Secrets and variables → Actions → Secrets)
  - EXPO_TOKEN
  - API_BASE_URL   (example: https://workspace.mdislam83.repl.co)
  - CI_CALLBACK_TOKEN  (generate random 32+ chars)
C) Server/Backend Environment Variables (where the API runs)
  - GITHUB_TOKEN  (PAT with repo + workflow scopes)
  - GITHUB_REPO
  - CI_CALLBACK_TOKEN (must match GitHub secret CI_CALLBACK_TOKEN)
Explain minimum scopes required for PAT.

DELIVERABLE 2 — GENERATE CI_CALLBACK_TOKEN SAFELY
Provide a safe one-liner command to generate a token:
- Linux/Mac: openssl rand -hex 32
- PowerShell: [guid]::NewGuid().ToString("N") + [guid]::NewGuid().ToString("N")
Do NOT output an actual token; only the command.

DELIVERABLE 3 — RUN REAL CI BUILD AND COLLECT PROOF
After secrets are configured, do the following and paste outputs:

1) Verify capabilities endpoint reflects CI configured:
curl -s https://<API_BASE_URL>/api/mobile/publish/capabilities \
 -H "Authorization: Bearer <ADMIN_TOKEN>" \
 -H "x-tenant-id: <TENANT_ID>"

2) Create or identify an existing publish job for target=expo android, then trigger CI:
curl -s -X POST https://<API_BASE_URL>/api/mobile/publish/jobs/<JOB_ID>/trigger-ci \
 -H "Authorization: Bearer <ADMIN_TOKEN>" \
 -H "x-tenant-id: <TENANT_ID>" \
 -H "Content-Type: application/json" \
 -d '{"target":"expo","platform":"android"}'

3) Poll CI status endpoint until success/fail:
curl -s https://<API_BASE_URL>/api/mobile/publish/jobs/<JOB_ID>/ci \
 -H "Authorization: Bearer <ADMIN_TOKEN>" \
 -H "x-tenant-id: <TENANT_ID>"

4) PROOF REQUIREMENTS (must paste raw):
A) GitHub Actions run URL (not secret)
B) Workflow log excerpt showing real command executed:
   - Expo: "eas build --platform android --non-interactive" (or equivalent)
   - Flutter: "flutter build appbundle" (or Gradle producing .aab)
C) API job updated to completed with artifacts where simulated=false
D) If artifacts are only in GitHub, attach via artifacts/attach endpoint with simulated=false and a GitHub artifact download URL (time-limited is ok).

DELIVERABLE 4 — STEP 30 PROOF PACK UPDATE
Update reports/step30-proof.md with:
- raw curl outputs (capabilities, trigger-ci, ci status)
- GitHub run URL + log excerpt
- artifact evidence (expo EAS build url or GH artifact url; flutter aab artifact url)
- smoke.sh full output
- verify.sh last 40 lines
- git status + commit hash

STOP
Do not proceed to Step 31.
If CI still cannot run, stop and output the exact blocker and the minimal fix.