You must produce PROOF-FIRST raw evidence for STEP 27: Flutter + FlutterFlow targets in Mobile Builder.
No claims without raw outputs. Do NOT start STEP 28.

ENV:
API=http://localhost:5000
WEB=http://localhost:3000
Use OWNER/ADMIN token and x-tenant-id.
Assume server already running.

DEFINITION OF DONE:
1) Expo target still works (15 files ZIP)
2) Flutter target works (ZIP contains pubspec.yaml + lib/main.dart etc)
3) FlutterFlow target works (ZIP contains export.json etc)
4) Rate limits: draft=20/hr, approve=30/hr, generate=10/hr (headers must show)
5) smoke.sh PASS (full output)
6) verify.sh last ~40 lines
7) reports/step27-proof.md created with all raw outputs and zip listings
8) git status shown

STEPS (MUST PASTE RAW OUTPUTS):

A) HEALTH + READY
1) curl -s $API/api/health
2) curl -s $API/api/ready

B) AUTH + RBAC (NO AUTH)
1) curl -i -X POST $API/api/mobile/spec/draft -H "Content-Type: application/json" -d '{"prompt":"test","target":"expo"}'
Paste full response headers+body (must be 401).

C) EXPO FLOW (RAW)
1) Draft:
curl -i -X POST $API/api/mobile/spec/draft \
-H "Authorization: Bearer $ADMIN_TOKEN" \
-H "x-tenant-id: $TENANT_ID" \
-H "Content-Type: application/json" \
-d '{"prompt":"Build a minimal app with login, preview, invite","target":"expo","appName":"expo_test"}'
Capture:
- SPEC_ID_EXPO from JSON
- Response headers containing RateLimit-* (must show 20/hr)

2) Approve:
curl -i -X POST $API/api/mobile/spec/approve \
-H "Authorization: Bearer $ADMIN_TOKEN" \
-H "x-tenant-id: $TENANT_ID" \
-H "Content-Type: application/json" \
-d '{"specId":"SPEC_ID_EXPO"}'
Capture headers (RateLimit-Limit must be 30/hr)

3) Generate:
curl -i -X POST $API/api/mobile/project/generate \
-H "Authorization: Bearer $ADMIN_TOKEN" \
-H "x-tenant-id: $TENANT_ID" \
-H "Content-Type: application/json" \
-d '{"specId":"SPEC_ID_EXPO"}'
Capture headers (RateLimit-Limit must be 10/hr)
Extract JOB_ID_EXPO + downloadUrl

4) Download + list zip:
curl -L -o /tmp/expo.zip "$API/api/mobile/download/JOB_ID_EXPO" \
-H "Authorization: Bearer $ADMIN_TOKEN" \
-H "x-tenant-id: $TENANT_ID"
unzip -l /tmp/expo.zip
Paste the unzip -l output (must show 15 files).

D) FLUTTER FLOW (RAW)
Repeat Draft/Approve/Generate/Download for target="flutter", appName="flutter_test".
Zip listing must include at least:
- pubspec.yaml
- lib/main.dart
- lib/routes.dart
- lib/screens/*
- l10n.yaml (or lib/l10n/*.arb)

E) FLUTTERFLOW FLOW (RAW)
Repeat Draft/Approve/Generate/Download for target="flutterflow", appName="ff_test".
Zip listing must include:
- export.json
- README_FLUTTERFLOW.md
- assets_manifest.json

F) RATE LIMIT BURST PROOF (for draft only)
Send 21 draft requests quickly for target="expo" and show:
- Request 20 OK with RateLimit-Remaining: 0
- Request 21 returns 429 with Retry-After: 3600 and route:"mobile/draft"
Paste the last 2 responses.

G) AUDIT LOG PROOF
Query DB or API (whatever exists in the project) and show last entries include:
- MOBILE_SPEC_DRAFT with target
- MOBILE_SPEC_APPROVE with target
- MOBILE_PROJECT_GENERATE with target
Paste raw results.

H) REGRESSION
1) ./scripts/smoke.sh (full output)
2) ./scripts/verify.sh (last ~40 lines)

I) ARTIFACTS + GIT
1) Create reports/step27-proof.md including all raw outputs above.
2) List changed files (if any) and show:
git status
git log -1 --oneline

STOP.